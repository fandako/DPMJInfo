<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dpmjinfo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".debug.MapFilterActivity"></activity>
        <activity android:name=".debug.MapKey">

            <!-- EMPTY -->
        </activity>
        <activity android:name=".debug.VehicleDetail">

            <!-- EMPTY -->
        </activity>
        <activity android:name=".debug.MapObjectSelection">

            <!-- android:parentActivityName=".MainActivity" -->
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <!--
            <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />
            -->
        </activity>
        <activity android:name=".debug.BusStopDetail">

            <!-- android:parentActivityName=".debug.MapObjectSelection" -->
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <!--
       <meta-data
       android:name="android.support.PARENT_ACTIVITY"
       android:value=".debug.MapObjectSelection" />
            -->
        </activity>
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>